import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  height: 4.5em;
  padding: 0.7em 4em;
  position: absolute;
  background-color: #4b8ab9;

  p {
    font-size: 1.2rem;
    font-weight: normal;
  }

  div {
    height: 100%;
    display: flex;
    color: #ffffff;
    font-weight: bold;
    align-items: center;
  }
`;
